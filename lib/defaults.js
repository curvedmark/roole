/**
 * Defaults
 *
 * A collection of default options.
 */
var defaults = exports

defaults.filePath = ''
defaults.imports = {}
defaults.prefix = ['webkit', 'moz', 'ms', 'o']
defaults.indent = '\t'
defaults.precision = 3